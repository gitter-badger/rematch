import setup_base

setup_base.build_setup(name='server',
                       package_name='rematch-server',
                       version_path='server',
                       package_base='server',
                       package_data={})
